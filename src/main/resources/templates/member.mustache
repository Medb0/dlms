{{>layout/admin_header}}
<div class="main_content">
    <div class="header">Welcome! DLMS System.</div>
    <div class="info">
        <div>
            <h2>회원관리</h2>
        </div>
        <div>
            <table class="reservation">
                <thead>
                <tr>
                    <th scope="cols" colspan="2">회원목록</th>
                </tr>
                </thead>
                <tbody>
                {{#posts}}
                    <tr>
                        <th scope="row">{{id}}</th>
                        <td>{{phone_num}}<button type=button onclick=main.delete({{id}})>회원탈퇴</button></td>
                    </tr>
                {{/posts}}
                </tbody>
            </table>
        </div>
    </div>
</div>
<script src="/scripts/member.js"></script>
{{>layout/admin_footer}}